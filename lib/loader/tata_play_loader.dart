
import 'package:flutter/material.dart';

class TataPlayLoader extends StatelessWidget {
  const TataPlayLoader({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset('assets/gifs/loader.gif'));
  }
}
